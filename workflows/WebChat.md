# n8n Chat Workflow (Groq AI)

## 📌 Назначение
Данный workflow реализует веб‑чат на базе **n8n**, который принимает сообщения через Webhook, обрабатывает их AI‑моделью **Groq**, и возвращает пользователю **чистый текстовый ответ** без JSON‑обёрток, кавычек и служебных сообщений.

Проект ориентирован на дальнейшую модернизацию:
- развитие веб‑чата (UI / UX)
- усложнение логики workflow
- добавление памяти, инструментов и маршрутизации

Перенос workflow между аккаунтами n8n **планируется позже**.

---

## 🧠 Архитектура workflow

```
Webhook
 → Edit Fields (подготовка input)
 → Groq Chat Model
 → Edit Fields (нормализация ответа)
 → Respond to Webhook
```

---

## 🔗 Webhook

**Настройки:**
- HTTP Method: `POST`
- Path: `/chat`
- Respond: **Using "Respond to Webhook" Node**

⚠️ Важно: Webhook **не должен** возвращать ответ напрямую, иначе в чат будет приходить служебное сообщение:
```json
{"message":"Workflow was started"}
```

---

## 🧩 Формирование messages

Используется строгий формат массива сообщений:

```json
messages: [
  {
    "role": "user",
    "content": "{{ $json.body.message }}"
  }
]
```

Ключевые правила:
- `messages` — **Array**
- каждый элемент — **Object**
- `content` всегда строка

---

## 🤖 AI (Groq)

- Используется **Groq Chat Model**
- Подключение через **n8n Credentials**
- Ответ модели извлекается из:

```
choices[0].message.content
```

---

## 🛠️ Нормализация ответа (Edit Fields)

Выполняется:
- извлечение текста ответа
- сохранение его в поле `answer`

Пример:
```js
answer = {{$json.choices[0].message.content}}
```

---

## 📤 Respond to Webhook (КРИТИЧЕСКИ ВАЖНО)

**Настройки:**
- Respond With: **Text**
- Response Body:
```
{{ $json.answer }}
```

❌ Не использовать:
```json
{ "answer": "..." }
```

Иначе фронт будет получать кавычки или пустые сообщения.

---

## ✅ Решённые проблемы

- ❌ Некорректный JSON Output
- ❌ `undefined` в expressions
- ❌ Ответы в кавычках (`""`)
- ❌ Служебные сообщения от Webhook
- ❌ HTML вместо JSON от внешних API

---

## 📈 Текущий статус

- ✅ Workflow стабилен
- ✅ Работает с веб‑чатом
- ✅ Подготовлен к расширению
- 🔜 Будущие шаги:
  - улучшение UI веб‑чата
  - память диалога
  - инструменты (search, weather, etc.)
  - условная логика

---

## 🧭 Рекомендации для будущей доработки

- Всегда контролировать JSON вручную
- Избегать автоматических режимов, если они ломают структуру
- Проверять output каждого узла
- Тестировать workflow через Test URL

---

## 🧑‍💻 Автор
Workflow собран и отлажен вручную с акцентом на инженерный контроль данных, а не визуальный конструктор.

---

> Этот README можно использовать как вводный контекст для любого агента или разработчика, который продолжит работу с данным workflow.

